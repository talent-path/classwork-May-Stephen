@model Student
@{
}

<h3>Are you sure you would like to delete
    @Model.Name from the student list?</h3>

@using (Html.BeginForm("Delete", "Student", FormMethod.Post))
            {
                @Html.HiddenFor(m => m.Id)
                <button>Yes</button>
            }
<button>No</button>
